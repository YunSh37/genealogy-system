# 家谱管理系统 — 后端代码实现简介

## 技术栈

| 技术 | 说明 |
|------|------|
| **语言** | C++17 |
| **Web 框架** | [Drogon](https://github.com/drogonframework/drogon)（高性能异步非阻塞 C++ Web 框架） |
| **数据库** | MySQL 8.0 |
| **JSON 库** | jsoncpp |
| **构建工具** | CMake 3.5+ |

## 项目结构

```
backend/
├── CMakeLists.txt                  # CMake 构建配置
├── main.cc                         # 服务入口，路由注册
├── .gitignore                      # 忽略构建产物和配置文件
├── import_data.sh                  # 数据导入脚本（CSV → MySQL）
├── export_data.sh                  # 数据导出脚本（MySQL → CSV）
├── export_backup.py                # Python 数据备份导出工具
├── controllers/                    # 控制器层（HTTP 请求处理）
│   ├── AuthController.cc/h         # 用户认证（注册/登录/登出）
│   ├── GenealogyController.cc/h    # 族谱 CRUD
│   ├── MemberController.cc/h       # 成员 CRUD + 批量查询 + 树数据
│   ├── RelationshipController.cc/h # 祖先/后代/亲缘关系/家族树
│   ├── StatisticsController.cc/h   # 统计分析（寿命/婚姻/出生）
│   ├── DashboardController.cc/h    # 族谱仪表盘统计
│   ├── ShareController.cc/h        # 族谱共享权限管理
│   └── ResponseUtils.cc/h          # 统一 JSON 响应格式
├── models/                         # 数据模型 + 工具
│   ├── Member.h                    # 成员模型（含 toJson 序列化）
│   ├── Genealogy.h                 # 族谱模型
│   ├── Marriage.h                  # 婚姻模型
│   ├── User.h                      # 用户模型
│   ├── StatsCache.h                # 统计缓存（内存 TTL 缓存）
│   ├── DbUtils.h                   # 数据库安全读取工具（防 NULL）
│   └── model.json                  # Drogon ORM 配置
├── migrations/                     # 数据库迁移脚本
│   ├── init_schema.sql             # 完整建表语句（6 张表 + 索引 + CHECK 约束）
│   ├── migration_phase3.sql        # Phase 3 性能优化（ancestor_path、FULLTEXT、缓存表）
│   ├── migration_fix_generations.sql # 辈分修复（迭代法）
│   ├── generate_test_data.py       # 测试数据生成（10 万+ 成员，最大深度 30）
│   └── phase3_performance.sql.py   # Python 版迁移脚本
├── filters/                        # Drogon 过滤器（预留）
├── plugins/                        # Drogon 插件（预留）
├── views/                          # Drogon 视图（预留）
└── test/                           # 单元测试
    ├── CMakeLists.txt
    └── test_main.cc
```

## 架构设计

```
HTTP 请求 → Drogon 路由 → Controller → DbClient (MySQL) → JSON 响应
                                ↓
                          models/ 数据模型（序列化）
```

- **路由注册**：通过 Drogon 的 `METHOD_LIST_BEGIN` / `ADD_METHOD_TO` 宏声明式注册，支持路径参数（`{genealogy_id}`、`{member_id}`）
- **数据库访问**：`app().getDbClient()->execSqlSync()` 同步执行 SQL，Drogon 框架保证非阻塞 I/O
- **认证鉴权**：Bearer Token 机制，`AuthController` 管理内存 Token 存储，各 Controller 通过 `getUserIdFromToken()` 验证身份
- **权限控制**：族谱创建者（owner）+ 共享权限（editor/viewer）三级权限模型

## API 接口一览

### 认证模块 (`/api/auth`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录（返回 Bearer Token） |
| POST | `/api/auth/logout` | 用户登出 |

### 族谱模块 (`/api/genealogy`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/genealogy` | 创建族谱 |
| GET | `/api/genealogy` | 获取族谱列表 |
| GET | `/api/genealogy/{id}` | 获取族谱详情 |
| PUT | `/api/genealogy/{id}` | 更新族谱信息 |
| DELETE | `/api/genealogy/{id}` | 删除族谱 |

### 成员模块 (`/api/member`, `/api/genealogy/{id}/member`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/genealogy/{id}/member` | 添加成员 |
| GET | `/api/genealogy/{id}/member` | 成员列表（分页） |
| GET | `/api/member/{id}` | 成员详情 |
| PUT | `/api/member/{id}` | 更新成员 |
| DELETE | `/api/member/{id}` | 删除成员 |
| GET | `/api/member/search` | 成员搜索（全文搜索） |
| GET | `/api/genealogy/{id}/tree` | 家族树精简数据（无分页） |
| POST | `/api/member/batch` | 批量查询成员 |

### 亲缘关系模块 (`/api/member`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/member/{id}/ancestors` | 查询祖先（支持无限深度） |
| GET | `/api/member/{id}/descendants` | 查询后代（支持无限深度） |
| GET | `/api/member/relationship` | 两个成员间的亲缘关系路径 |
| GET | `/api/member/{id}/family-tree` | 完整家族树（祖先+后代） |
| GET | `/api/genealogy/{id}/tree-layers` | 分层树加载（性能优化） |

### 统计分析模块 (`/api/genealogy/{id}/stats`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/genealogy/{id}/stats` | 族谱统计仪表盘 |
| GET | `/api/genealogy/{id}/stats/longest-lived-generation` | 平均寿命最长的一代 |
| GET | `/api/genealogy/{id}/stats/unmarried-men-over-50` | 50岁以上未婚男性 |
| GET | `/api/genealogy/{id}/stats/born-before-average` | 早于平均出生年份的成员 |
| GET | `/api/genealogy/{id}/sql-demo` | SQL 核心功能演示 |

### 共享模块 (`/api/genealogy/{id}/share`)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/genealogy/{id}/share` | 添加共享用户 |
| GET | `/api/genealogy/{id}/share` | 获取共享用户列表 |
| PUT | `/api/genealogy/{id}/share/{uid}` | 更新共享权限 |
| DELETE | `/api/genealogy/{id}/share/{uid}` | 删除共享用户 |

### 系统接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 服务信息 |
| GET | `/api/health` | 健康检查（轻量，不查数据库） |

## 核心性能优化

### 1. 祖先查询：Materialized Path

**原理**：每行存储 `ancestor_path` 字段（如 `/1/862/2717/33332`），查询祖先时直接解析该字段，无需递归 CTE。

```
优化前: 递归 CTE（深度30代超时）
优化后: SELECT ancestor_path FROM Member WHERE member_id=? → 解析字符串 → 批量查询
性能: 从超时 → 毫秒级
```

### 2. 后代查询：双层策略

- **根节点快速通道**：利用 `idx_member_genealogy_gen(genealogy_id, generation)` 复合索引，一条 SQL 范围扫描

```sql
SELECT ... FROM Member
WHERE genealogy_id = ? AND generation > ? AND generation <= ?
ORDER BY generation
```

- **通用迭代通道**：利用 `father_id`/`mother_id` 索引逐层 IN 查询，每层精确命中

```
优化前: 递归 CTE（深度30代超时2分钟+）
优化后: 根节点 48K 后代仅需 1.88s（SQL ~50ms，其余为 JSON 序列化）
```

### 3. 亲缘关系查询：批量替代 N+1

**原理**：收集所有需要的成员 ID → 一次 `WHERE member_id IN (...)` 批量查询 → 内存 map 查找。

```
优化前: 1065 次 SQL（单独查询每个成员 + 配偶）
优化后: 3~4 次 SQL
```

### 4. 家族树查询：消除 N+1 + 深度限制修复

**问题诊断**（修复前）：
- 深度硬上限 20 代（`if (depth > 20) depth = 20`），后代最多只到 21 代
- N+1 配偶查询：为每个祖先和后代单独查询配偶（48K 后代 = 48,000+ 次 SQL）
- 祖先路径 while 循环 N+1：每代 1 次 SQL（30 代 = 30 次）

**修复方案**：
- 移除深度硬上限，改为 `depth > 100`（0=不限，与独立后代查询一致）
- `getAncestorLineagePath` 改用 `ancestor_path` 解析（1 次 SQL 替代 N 次）
- 批量查询所有成员后从结果中提取 `spouse_id`，仅对缺失配偶做第二轮小批量补查
- 总 SQL：**1065 次 → 3~4 次**

**修复后性能**：
```
根节点 depth=30: 3.6s（48,314 后代 + 祖先链 + 全部配偶）
非根节点 depth=10: 101ms（1 祖先 + 1,190 后代）
```

### 5. 其他优化

- **全文搜索**：MySQL FULLTEXT 索引替代 `LIKE '%keyword%'`，支持中文分词
- **统计缓存**：内存 TTL 缓存（`StatsCache`），避免重复计算
- **批量接口**：`/api/member/batch` 支持一次查询最多 5000 个成员
- **精确列选择**：避免 `SELECT *`，不传输 TEXT 类型的 `biography` 字段

## 数据库设计

### 核心表

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `User` | 用户表 | user_id, username, password |
| `Genealogy` | 族谱表 | genealogy_id, name, surname, user_id |
| `Member` | 成员表 | member_id, father_id, mother_id, spouse_id, generation, ancestor_path |
| `Marriage` | 婚姻表 | marriage_id, husband_id, wife_id |
| `GenealogyShare` | 共享表 | share_id, genealogy_id, user_id, permission |
| `StatisticsCache` | 缓存表 | cache_key, cache_value (JSON), expires_at |

### 关键索引

- `idx_member_genealogy_gen(genealogy_id, generation)` — 根节点后代快速通道
- `idx_member_ancestor_path(ancestor_path)` — 路径前缀查询
- `ft_member_name(name)` — 全文搜索
- `idx_member_father_id(father_id)` / `idx_member_mother_id(mother_id)` — 父子关系查询

## 数据导入导出

### 导入 (`import_data.sh`)

```bash
# 先设置环境变量
export MYSQL_HOST=127.0.0.1
export MYSQL_PORT=3306
export MYSQL_USER=root
export MYSQL_PASSWORD=your_password
export MYSQL_DATABASE=genealogy_db

# 将 CSV 文件导入 MySQL
./import_data.sh /path/to/your/csv/files

# 查看帮助
./import_data.sh --help
```

### 导出 (`export_data.sh`)

```bash
# 全量导出族谱
./export_data.sh --full <genealogy_id>

# 分支导出（指定根成员）
./export_data.sh --branch <genealogy_id> <root_id>

# 导出所有族谱
./export_data.sh --all

# 列表查看
./export_data.sh --list
```

导出格式：CSV（含表头），导出目录 `exports/`。

## 构建运行（WSL2 / Linux）

> 以下流程假定使用 **WSL2 (Ubuntu 22.04)**。纯 Linux 环境操作完全相同，跳过 WSL 特定步骤即可。

### 环境要求

- **系统**：WSL2（推荐 Ubuntu 22.04）或原生 Linux
- **编译器**：GCC 9+ 或 Clang 10+（需支持 C++17）
- **数据库**：MySQL 8.0+
- **依赖**：CMake 3.5+、jsoncpp、libmysqlclient-dev、Python 3.8+

---

### 第 0 步：进入 WSL 并定位到项目目录

```bash
# 在 Windows 终端中进入 WSL
wsl

# 切换到后端目录（假设项目在 D:\shujuku\tmp\genealogy-system）
cd /mnt/d/shujuku/tmp/genealogy-system/backend
```

> 后续所有操作均在 **WSL 终端**的 `backend/` 目录下执行。

---

### 第 1 步：安装系统依赖（仅首次）

```bash
# 更新包列表
sudo apt update

# 安装编译工具链
sudo apt install -y build-essential cmake libjsoncpp-dev

# 安装 MySQL 客户端库
sudo apt install -y libmysqlclient-dev

# 安装 Drogon Web 框架（从源码编译，仅需一次）
cd /tmp
git clone https://github.com/drogonframework/drogon.git
cd drogon && mkdir build && cd build
cmake .. && make -j$(nproc) && sudo make install
```

---

### 第 2 步：启动 MySQL 并创建数据库

```bash
# 启动 MySQL 服务（WSL 中每次重启后都要执行）
sudo service mysql start
```

**接下来需要连接 MySQL。先快速测试是否需要密码：**

```bash
sudo mysql -u root -e "SELECT 1" 2>/dev/null && echo "→ 无密码，情况 A" || echo "→ 需要密码，情况 B"
```

**情况 A — 输出"无密码"（WSL 默认安装）：**

```bash
sudo mysql -u root -e "CREATE DATABASE IF NOT EXISTS genealogy_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
sudo mysql -u root -e "ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '123456'; FLUSH PRIVILEGES;"
```

**情况 B — 输出"需要密码"（安装时设过密码）：**

```bash
sudo mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS genealogy_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
sudo mysql -u root -p -e "ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '123456'; FLUSH PRIVILEGES;"
```

> 无论哪种情况，完成后 MySQL root 密码统一为 `123456`。后续命令不再需要区分。

---

### 第 3 步：执行建表脚本

```bash
# 回到 backend/ 目录
cd /mnt/d/shujuku/tmp/genealogy-system/backend

# 执行完整建表脚本（6 张表 + 13 个索引 + CHECK 约束）
mysql -u root -p123456 genealogy_db < migrations/init_schema.sql

# 执行性能优化迁移（ancestor_path 物化路径 + 全文索引 + 统计缓存表）
mysql -u root -p123456 genealogy_db < migrations/migration_phase3.sql

# 修复辈分计算（可选，CSV 导入后如辈分不正确时执行）
mysql -u root -p123456 genealogy_db < migrations/migration_fix_generations.sql
```

---

### 第 4 步：生成测试数据

```bash
cd migrations

# 运行数据生成脚本（生成 CSV 文件到 migrations/test_data/ 目录）
python3 generate_test_data.py

# 生成的数据量：10 个族谱、100,534 名成员、~50,000 条婚姻、最大深度 30 代

cd ..
```

---

### 第 5 步：导入数据到 MySQL

```bash
# 导出密码环境变量（import_data.sh 自动读取，跳过交互提示）
export MYSQL_PASSWORD=123456

# 将生成的 CSV 文件导入数据库
./import_data.sh migrations/test_data
```

> **手动密码模式**：不设置 `MYSQL_PASSWORD` 环境变量，脚本会交互式询问密码；无密码时直接回车，脚本自动使用 `sudo mysql`。
> **自定义数据**：将 `migrations/test_data` 替换为你的 CSV 数据目录路径。

---

### 第 6 步：编译后端程序

```bash
# 在 backend/ 目录下
mkdir -p build && cd build
cmake ..
cmake --build .
```

编译成功后在 `build/` 目录下生成 `genealogy_system` 可执行文件。

> 如果编译报错找不到 drogon，检查 `/usr/local/lib` 是否在库搜索路径中：
> ```bash
> sudo ldconfig
> ```

---

### 第 7 步：创建配置文件

在 `backend/` 目录下创建 `config.json`：

```json
{
    "listeners": [
        {
            "address": "0.0.0.0",
            "port": 8088
        }
    ],
    "db_clients": [
        {
            "name": "default",
            "rdbms": "mysql",
            "host": "127.0.0.1",
            "port": 3306,
            "dbname": "genealogy_db",
            "user": "root",
            "password": "123456",
            "client_encoding": "utf8mb4"
        }
    ],
    "app": {
        "number_of_threads": 4
    }
}
```

---

### 第 8 步：启动后端服务

```bash
cd backend/build
./genealogy_system
```

看到 `INFO - Listening on 0.0.0.0:8088` 表示启动成功。

---

### 验证服务

```bash
# 在另一个 WSL 终端中测试
curl http://localhost:8088/api/health
# 返回: {"status":"ok"}

curl http://localhost:8088/
# 返回服务基本信息

# 在 Windows 浏览器中访问 http://localhost:8088
```

---

### 完整流程速查

```
# ===== 后端完整部署流程（WSL2 / Linux）=====
# ★ 如果 sudo mysql 报 Access denied，需要加 -p 并输入 MySQL 密码
wsl                                                          # 进入 WSL
cd /mnt/d/shujuku/tmp/genealogy-system/backend               # 定位目录
sudo service mysql start                                      # 启动 MySQL
# 自适应连接（无密码直接用，有密码则提示输入）
sudo mysql -u root -e "SELECT 1" 2>/dev/null && \
  (sudo mysql -u root -e "CREATE DATABASE IF NOT EXISTS genealogy_db CHARACTER SET utf8mb4;" && \
   sudo mysql -u root -e "ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '123456'; FLUSH PRIVILEGES;") || \
  (sudo mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS genealogy_db CHARACTER SET utf8mb4;" && \
   sudo mysql -u root -p -e "ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '123456'; FLUSH PRIVILEGES;")
mysql -u root -p123456 genealogy_db < migrations/init_schema.sql    # 建表
mysql -u root -p123456 genealogy_db < migrations/migration_phase3.sql  # 迁移
cd migrations && python3 generate_test_data.py && cd ..        # 生成数据
export MYSQL_PASSWORD=123456 && ./import_data.sh migrations/test_data  # 导入
mkdir -p build && cd build && cmake .. && cmake --build .      # 编译
cd .. && cat > config.json << 'EOF'                            # 配置
{
    "listeners": [{"address": "0.0.0.0", "port": 8088}],
    "db_clients": [{"name": "default", "rdbms": "mysql", "host": "127.0.0.1", "port": 3306,
      "dbname": "genealogy_db", "user": "root", "password": "123456", "client_encoding": "utf8mb4"}],
    "app": {"number_of_threads": 4}
}
EOF
cd build && ./genealogy_system                                 # 启动
```

## 安全设计

- **认证**：Bearer Token 内存管理，支持登出失效
- **密码**：SHA-256 哈希存储（通过 `encrypt_password`/`decrypt_password`）
- **权限**：族谱级三级权限（owner > editor > viewer），成员操作继承族谱权限
- **SQL 注入防护**：全部使用参数化查询（`?` 占位符），杜绝拼接 SQL
- **约束**：数据库层 CHECK 约束（辈分 > 0、出生日期 ≤ 死亡日期、ancestor_path 格式）

## 性能基准

测试环境：WSL2 Ubuntu, MySQL 8.0, 10 个族谱, 100,534 名成员, 最大深度 30 代。

| 场景 | 数据量 | 响应时间 | 说明 |
|------|--------|----------|------|
| 根节点后代 depth=5 | ~8,000 | **0.47s** | 含 JSON 序列化 |
| 根节点后代 depth=30 | ~48,000 | **1.88s** | SQL ~50ms，其余为序列化 |
| 非根节点后代 depth=10 | ~1,700 | **0.12s** | 迭代法批量查询 |
| 祖先追溯（到根） | 21 代 | **<10ms** | ancestor_path 解析 |
| 家族树 depth=30 | ~48,000 后代 + 祖先链 | **3.56s** | 含全部后代配偶信息 |
| 家族树 depth=10 | ~1,200 | **0.10s** | 含祖先+后代+配偶 |
| 亲缘关系查询 | — | **<50ms** | 批量替代 N+1 |
| 成员搜索（全文） | 100K+ | **<100ms** | FULLTEXT 索引 |
| 族谱列表 | 10 | **<10ms** | — |
